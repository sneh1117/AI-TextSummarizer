services:
  - type: web
    name: ai-text-summarizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1 --bind 0.0.0.0:10000
    plan: free